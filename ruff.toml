line-length = 88
target-version = "py311"

[lint]
select = ["E", "F", "I", "UP", "B", "SIM"]
# ruff format governs the width of code. Every remaining long line is a SQL or
# Markdown string literal, which the formatter cannot split.
ignore = ["E501"]
