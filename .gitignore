# Python
__pycache__/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
coverage.xml

# pixi environments
.pixi/

# Jupyter
.ipynb_checkpoints/

# Agent and editor state
.serena/
# Claude Code worktrees: nested checkouts, never part of this repo.
.claude/worktrees/
# Personal Claude Code settings. The shared ones live in .claude/settings.json.
.claude/settings.local.json

# Vale packages fetched by `vale sync`.
styles/Google/
styles/Microsoft/
styles/Readability/
styles/ai-tells/
# `vale sync` also unpacks the ai-tells agent template here.
styles/config/templates/

# Downloaded and derived analysis inputs. estimate.py writes them.
data/
# Analysis results. estimate.py writes them, and the Zenodo record holds
# the published copy.
outputs/

# OS
.DS_Store

# External literature review, not for publication with the repo.
docs/argentina_census_accuracy_literature_review_final.docx
